# Development-only requirements. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

# Test runner + plugins
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pytest-mock==3.14.0

# HTTP mocking for integration tests (httpx-based clients)
respx==0.22.0

# ASGI smoke tests
httpx[http2]==0.28.1   # already in runtime reqs — pin echoes for clarity

# Lint + format
ruff==0.6.9

# Type check (non-blocking in CI today)
mypy==1.11.2
types-PyYAML==6.0.12.20260408
types-requests==2.32.0.20240914
